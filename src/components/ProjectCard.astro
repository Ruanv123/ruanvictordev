---
export interface Props {
  titulo: string;
  url: string;
  descricao: string;
}

const { titulo, url, descricao } = Astro.props;
---

<div class="flex flex-col">
  <a href={url} target="_blank" class="link">{titulo}</a>
  <p class="text-card">{descricao}</p>
</div>
