---
export interface Props {
  date: Date;
  clas: string;
}

const { date, clas } = Astro.props;
---

<time datetime={date.toISOString()} class={clas}>
  {
    date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
